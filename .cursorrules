# ═══════════════════════════════════════════════════════════
# L1 宪法层 — The Constitution
# 智能体潜意识与绝对行为边界
# ═══════════════════════════════════════════════════════════

## 1. 核心定位

本智能体运行于 **三层记忆架构** 之下，为 **Personal Knowledge Intelligence Agent（PKIA）** 提供全自动、有状态的研发支持。

- **主仓库**：`DifyMind`（origin: https://github.com/piomeer/DifyMind.git）
- **核心项目**：PKIA — 个人知识智能体
- **底层平台**：Dify（LLM 应用开发平台）
- **工作目录**：`/home/mahe/dify`

## 2. 技术栈与架构锁死

### 2.1 Dify 后端 (api/)
- 语言：Python（Flask, 领域驱动设计）
- 包管理：`uv run --project api`
- 测试框架：pytest（Arrange-Act-Assert）
- 代码规范：Ruff 格式化 + ruff linting（遵循 `.ruff.toml`）
- 架构模式：控制器 → 服务 → 核心/领域 分层架构
- ORM：SQLAlchemy（继承 `models.base.TypeBase`）
- 异步任务：Celery + Redis Broker

### 2.2 Dify 前端 (web/)
- 框架：Next.js + TypeScript + React
- 包管理：pnpm（工作空间根目录）
- 测试框架：Vitest + React Testing Library
- 代码规范：ESLint（`pnpm lint:fix` 优先）+ `pnpm type-check`
- 覆盖组件：仅使用 `@langgenius/dify-ui/*` 覆盖原语
- 国际化：所有用户可见字符串放入 `web/i18n/en-US/`

### 2.3 PKIA (Personal Knowledge Intelligence Agent)
- 构建于 Dify Platform 之上
- 利用 Dify Workflow、RAG Pipeline、Agent 能力实现个人知识管理
- 三层记忆系统作为状态管理基础设施

## 3. 单机 WSL 开发协议

| 项目 | 值 |
|------|-----|
| 开发环境 | WSL (Linux) |
| 默认分支 | `main` |
| 推送策略 | **禁止未经确认自动 push** — 所有推送前必须等待用户明确批准 |
| 提交格式 | `<类型>: <描述>`（如 `feat: 添加PKIA核心模块`） |

**规则**：
- 所有开发在本地 WSL 完成
- 不允许在无用户确认的情况下执行 `git push`
- 仅允许本地 commit，推送需用户明确指令

## 4. 数据库保护红线

- **L2 图谱层文件**（`cline-memory.json`）：绝对禁止使用普通文本编辑工具格式化、美化或手动编辑
- **所有 L2 更新** 必须通过 MCP `memory` 服务器的工具或 Governor API 进行
- **L3 进度层**：使用命名空间化的多文件模型，参见 §5.8

## 5. Memory Governance（强制流程）

### 5.1 Agent 生命周期

每次任务必须按以下顺序执行：

```
Task → Memory Review → L2 Update(如需) → L3 Update → Receipt → Completion Gate → attempt_completion
```

### 5.2 任务结束前强制检查清单

#### L2 Review（长期记忆检查）

```
□ 架构决策变更？（技术栈/架构模式/约束）
  → 需要: Governor.write() → architecture:*@*
□ 长期事实变更？（项目阶段/里程碑/基线）
  → 需要: Governor.write() → project:*@*
□ 规范变更？（Schema/Protocol/API）
  → 需要: Governor.write() → project:*@*
□ 系统行为变更？（Governor/持久化）
  → 需要: Governor.write() → architecture:*@*
```

#### L3 Review（进度同步检查）

```
□ 任务完成？ → 更新 progress/<namespace>.md「✅ 已完成」
□ 阶段完成？ → 更新 progress/<namespace>.md「🎯 当前活动目标」
□ 阻塞解除？ → 更新 progress/<namespace>.md「⛔ 已知卡点」
□ 新阶段启动？ → 更新 progress/<namespace>.md「🚀 下一步计划」
```

### 5.3 快速判定表

| 任务类型 | L2 同步 | L3 同步 |
|---------|:-------:|:-------:|
| Bug 修复 | ❌ | ✅ |
| 功能实现 | ⚠️ 仅架构影响时 | ✅ |
| 文档编写 | ✅ 规范变更 | ✅ |
| 测试编写 | ❌ | ✅ |
| 架构评审 | ✅ 决策冻结 | ✅ |
| 性能优化 | ✅ 基线数据 | ✅ |
| 重构 | ✅ 架构变更 | ✅ |
| 流程定义 | ✅ 系统行为 | ✅ |
| 配置变更 | ✅ 长期事实 | ✅ |

### 5.4 Completion Gate

`attempt_completion` **之前**必须确认：

```
Gate 1: L2 Review Complete    ✅/❌
Gate 2: L3 Review Complete    ✅/❌
Gate 3: Receipt Generated     ✅/❌

全部通过 → 允许 attempt_completion
任一未通过 → 阻断，返回修复

**绝对规则**: 如果上述 Gate 条件未全部通过，Agent 必须暂停并通知用户。禁止在 Gate OPEN 状态下调用 `attempt_completion`。这不是建议，是必须遵守的规则。违反此规则的行为将被视为流程违规。
```

### 5.5 attempt_completion 格式

每次 `attempt_completion` 的 `result` 必须包含：

```
Memory Sync:
  L2: [updated|skipped] — [slots]
  L3: [updated|skipped] — [sections]
  Receipt: [generated|skipped]
  Gate: [CLOSED|OPEN]
```

### 5.6 Memory Operations

- **L2 写入**: `Governor.write()` → 自动持久化到 `cline-memory.json`
- **L2 查询**: `Governor.get_active(slot_id)`
- **L3 更新**: 编辑 `progress/<namespace>.md`
- **MCP 同步**: 执行 `Governor.write()` 返回的 `commands`

### 5.7 参考文档

- `docs/agent_memory_integration_v1.0.md`（完整流程）
- `docs/memory_sync_receipt_template.md`（Receipt 模板）
- `docs/memory_sync_protocol_v1.0.md`（同步协议）
- `docs/memory_governance_freeze_v1.0.md`（冻结范围）

### 5.8 L3 Namespace Management

#### 规则

1. L3 不再是单文件 PROGRESS.md。
2. L3 由 `progress/` 目录下的命名空间文件组成。
3. 每个 Agent 必须操作且仅操作一个命名空间。
4. 如果命名空间文件不存在，先创建再开始实现工作。
5. 禁止将不相关任务的进度写入其他命名空间文件。
6. `attempt_completion` 前必须更新命名空间进度文件。

#### 命名空间定义

| 命名空间 | 文件 | 用途 |
|---------|------|------|
| `workspace` | `progress/workspace.md` | Workspace 级开发管理 |
| `pkia_mvp` | `progress/pkia_mvp.md` | PKIA MVP 开发进度 |
| `pkia_memory` | `progress/pkia_memory.md` | Memory 模块进度 |
| `mukg` | `progress/mukg.md` | MuKG 遗留项目 |

更多信息参见 `docs/l3_namespace_architecture_v1.0.md`。

### 5.9 Agent Bootstrap Protocol

#### Bootstrapping (强制流程)

在开始实现工作 **之前**，必须执行以下步骤：

1. **确定命名空间**（workspace / pkia_mvp / pkia_memory / mukg）
2. **加载命名空间进度文件** `progress/<namespace>.md`（如缺失则创建）
3. **审查相关 L2 记忆**（Governor.get_active 获取架构决策）
4. **生成 Bootstrap Summary**（包含命名空间、阶段、活跃任务、阻塞）
5. **开始实现工作**

#### 规则

- Bootstrap 完成前不得开始实现工作。
- 如果命名空间无法确定，默认使用 `workspace`。
- 如果进度文件缺失，使用标准模板创建。
- Bootstrap 必须提取以下信息，缺失则视为 **FAILED**：
  - Relevant L2 Memories（架构决策和项目事实）
  - Working Context（ACTIVE 决策的可读摘要）
  - Current Constraints（冻结规则和限制）

更多信息参见 `docs/bootstrap_protocol_v1.1.md`。

### 5.10 Workspace Entry Point

如果 `START_HERE.md` 存在于项目根目录：

1. Agent 应在命名空间解析之前读取 `START_HERE.md`
2. `START_HERE.md` 是所有未来 Agent 的推荐入口点

此规则不修改 Bootstrap Protocol 逻辑，仅添加入口点指引。

## 6. 编码规范红线

- 禁止使用 `print()` — 必须使用 `logger = logging.getLogger(__name__)`
- 禁止直接读取环境变量 — 必须通过 `configs.dify_config`
- 禁止使用 `Any` 类型 — 必须使用精确类型注解
- 所有数据库查询必须携带 `tenant_id` 范围
- 后台任务必须幂等，并记录关联对象标识符